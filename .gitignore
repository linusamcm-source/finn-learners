node_modules/
dist/
# Development aid, regenerated by `just seed-demo`.
demo-progress.json

# Ingestion working files: the handbook PDF is downloaded, not vendored.
ingest/.venv/
ingest/downloads/
__pycache__/
.DS_Store
ingest/fixtures/

# Derived from the Crown-copyright handbook PDF, so not committed. Both are
# rebuilt by `just ingest`; assets/handbook/.gitkeep keeps the directory.
content/handbook.json
assets/handbook/*.png
