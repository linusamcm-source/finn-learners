[project]
name = "learner-dash-ingest"
version = "0.1.0"
description = "One-off extraction of the Tasmanian Road Rules Handbook into structured JSON"
requires-python = ">=3.11"
dependencies = [
    "pdfplumber>=0.11.4",
    "httpx>=0.27.0",
    "pillow>=10.4.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "fpdf2>=2.8.8",
]
